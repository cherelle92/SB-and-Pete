<%# <h1>Homepage#index</h1>
<p>Find me in app/views/homepage/index.html.erb</p> %>

<h1>Movie Mood app</h1>

<div>
<h3>What are you in the mood for?</h3>
    <form action="/homepage/select" method="post">
      <% @moods.each do |m| %>
				<input type="radio" id="<%=m.mood%>" name="mood_id" value="<%=m.id%>">
        <label for="<%=m.mood%>"> <%=m.mood%> </label>
				
        <br>
      <% end %>
      <input type="submit" value="Pick">
     <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
    </form>
</div>

 